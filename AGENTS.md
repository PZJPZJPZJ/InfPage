# InfPage 项目协作规范

## 项目定位与目录

- 本项目是中文 VuePress 信息站，内容分为三类并严格隔离：
  - `src/wiki/`：软件、系统、开发等文档教程。
  - `src/note/`：笔记、分析与经验总结。
  - `src/tool/`：可直接在浏览器使用的交互式小工具。
- 新内容应放入对应分类；工具页面不要混入长篇教程，说明性内容应放到 `wiki` 或 `note`。

## 工具模块

- 一个工具只能由 `src/tool/<分类>/<slug>.md` **单个 Markdown 文件**实现，不新建同工具的 `.vue`、`.js`、`.css`、资源目录或共享组件。
- 工具必须零新增依赖：只使用 Vue、浏览器原生 Web API 和项目已配置能力；不得修改 `package.json`、锁文件。
- 文件以 `routeMeta`（`itemTitle`、`itemDesc`、`itemIcon`）开头，随后是中文标题、必要的简短说明、界面、`<script setup>` 与 `<style scoped>`。
- 可以直接在 Markdown 中书写 Vue 模板语法和 `<script setup>`，但**不要写最外层 `<template>` 标签**。
- 工具页面中，除 `<script setup>` 与 `<style scoped>` 外，一级标题使用 Markdown，说明和界面一律使用 HTML 标签书写；不得直接书写裸露的 Markdown 文案，也不得使用 HTML 注释。
- 除页面唯一的 Markdown 一级标题外，工具界面内部不使用 `h1`–`h6` 标签，避免受到 VuePress 标题锚点、目录收集和主题间距影响；分区标题优先使用带专属 class 的 `<p>`、`<div>` 或 `<strong>`，需要建立可访问性关联时配合 `id`、`aria-labelledby` 等属性。
- HTML 应按结构层级和属性长度合理分行、缩进，避免将复杂界面压缩为难以维护的单行代码；但 HTML 界面内部不得出现完全空白行，防止 VuePress 将连续标签解析为不同的 Markdown 区块。
- HTML 页面内容结束后与 `<script setup>` 之间必须保留一个空行，`</script>` 与 `<style scoped>` 之间也必须保留一个空行；除这两个分隔位置外，不使用完全空白行组织工具页面的 HTML。
- 浏览器 API、网络请求和 `window`/`document` 访问须兼顾 SSR：在挂载后执行或先判断浏览器环境；权限、加载、空数据和错误状态必须对用户可见，并在卸载时清理监听器、计时器与动画帧。

## UI 与交互规范

- 设计基准为 **VuePress Default Theme + 卡片式 Liquid Glass**。页面以克制的半透明卡片组织内容，通过高光边框、低半径背景模糊和轻微染色形成玻璃层次；避免大面积强模糊、过度透明、霓虹光晕和影响阅读的复杂渐变。
- 优先复用主题令牌：`--vp-c-bg`、`--vp-c-bg-soft`、`--vp-c-bg-mute`、`--vp-c-text-1/2/3`、`--vp-c-border`、`--vp-c-brand`、`--vp-c-accent*`。玻璃背景、边框、阴影和状态色应由这些令牌派生，并分别适配浅色与深色主题，不硬编码整套主题颜色。
- 跨工具复用的玻璃卡片、分组容器、按钮、输入框、状态提示等基础样式，应优先统一追加到 `src/.vuepress/styles/index.scss`，共享类统一使用 `.vp-custom-*` 前缀；不得为此修改文件中已有的 `:root` 或主题根变量。单个工具只在自身 `<style scoped>` 中保留专属布局、尺寸修正和确实无法复用的个别样式，不重复实现相同视觉规则。
- 玻璃卡片默认使用细且明亮的半透明边框、约 `6px–12px` 的低半径 `backdrop-filter` 模糊、轻背景色和克制阴影；圆角建议为 `12px–16px`，嵌套控件圆角应略小。必须提供不支持 `backdrop-filter` 时仍清晰可读的实色或半透明背景回退。
- 信息层级依靠背景明度、边框强弱、间距和字号共同表达，不只依赖透明度。主操作使用品牌/强调色，次要操作使用弱染色玻璃或高光描边，危险操作使用明确的红色语义与文字提示；数据、时间和金额使用等宽数字或 `font-variant-numeric: tabular-nums`。
- 控件高度、圆角、边框与交互反馈应统一，点击区域不小于约 `40px`。悬停时只做轻微提亮或边框增强，按下状态需有明确压感；键盘焦点必须可见，禁用、加载、空数据、成功和失败状态必须同时提供文字或图标说明。
- 同时适配浅色和深色主题，正文、弱文本、输入值与玻璃背景之间应满足 WCAG 2.1 AA 的可读性目标。不得只靠颜色传递状态，也不得因透明效果让下层内容干扰当前卡片阅读。
- 响应式优先保证手机可用：桌面可使用 Grid/Flex 多列卡片，在约 `720px` 以下收为单列；减少移动端的模糊和阴影开销，避免固定宽度、横向溢出及过密排列。
- 动效以 `120ms–200ms` 的颜色、边框、阴影和轻位移过渡为主，并尊重 `prefers-reduced-motion`。避免持续漂浮、循环高光、自动跳转和无确认的破坏性操作。

## VuePress 与变更边界

- 未经用户明确允许，不得修改 VuePress 核心配置和基础设施：`src/.vuepress/config.js`、`src/.vuepress/client.js`、主题别名/覆盖组件、构建脚本、`package.json` 与锁文件。`src/.vuepress/styles/index.scss` 仅允许用于维护跨工具复用的主题令牌与共享基础样式，不应加入只服务于单个工具的规则。
- 不得为单个工具添加全局 CSS、插件、路由配置或构建配置；目录约定和自动发现机制应保持不变。
- 修改完成后，优先运行与改动相称的检查；工具改动至少确认构建不报错，并检查桌面与移动端布局及深浅色主题。
