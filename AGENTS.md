# InfPage 项目协作规范

## 项目定位与目录

- 本项目是中文 VuePress 信息站，内容分为三类并严格隔离：
  - `src/wiki/`：软件、系统、开发等文档教程。
  - `src/note/`：笔记、分析与经验总结。
  - `src/tool/`：可直接在浏览器使用的交互式小工具。
- 新内容应放入对应分类；工具页面不要混入长篇教程，说明性内容应放到 `wiki` 或 `note`。

## 工具模块

- 一个工具只能由 `src/tool/<分类>/<slug>.md` **单个 Markdown 文件**实现，不新建同工具的 `.vue`、`.js`、`.css`、资源目录或共享组件。
- 工具必须零新增依赖：只使用 Vue、浏览器原生 Web API 和项目已配置能力；不得修改 `package.json`、锁文件，也不得引入 CDN、第三方脚本或 SDK。
- 文件以 `routeMeta`（`itemTitle`、`itemDesc`、`itemIcon`）开头，随后是中文标题、必要的简短说明、界面、`<script setup>` 与 `<style scoped>`。
- 可以直接在 Markdown 中书写 Vue 模板语法和 `<script setup>`，但**不要写最外层 `<template>` 标签**。
- 工具页面中，除 `<script setup>` 与 `<style scoped>` 外，一级标题使用Markdown，说明和界面一律使用 HTML 标签书写；不得直接书写裸露的 Markdown 文案。HTML 标签之间不得保留空行，也不得使用 HTML 注释；页面内容结束后与 `<script setup>` 之间保留一行空行。
- 浏览器 API、网络请求和 `window`/`document` 访问须兼顾 SSR：在挂载后执行或先判断浏览器环境；权限、加载、空数据和错误状态必须对用户可见，并在卸载时清理监听器、计时器与动画帧。

## UI 与交互规范

- 设计基准为 **VuePress Default Theme + 轻量工具面板**，优先复用主题令牌：`--vp-c-bg`、`--vp-c-bg-soft`、`--vp-c-bg-mute`、`--vp-c-text-1/2/3`、`--vp-c-border`、`--vp-c-brand`、`--vp-c-accent*`。不要硬编码整套浅色/深色背景，也不要覆盖站点全局样式。
- 每个工具使用自身语义化、唯一前缀的类名（例如 `totp-*`、`sw-*`），样式必须 `scoped`；卡片采用柔和边框、8–16px 圆角，间距以 4px/8px 网格递进，按钮和输入控件保持一致高度与清晰焦点态。
- 信息层级清晰：主操作使用品牌/强调色，次要操作使用描边或弱背景，危险操作使用红色并需明确文案；数据、时间和金额使用等宽数字或 `font-variant-numeric: tabular-nums`。
- 同时适配浅色和深色主题；正文与背景、交互控件的文字与背景应满足 WCAG 2.1 AA 的可读性目标。不要只靠颜色传递状态，应配合文字、图标或形状。
- 响应式优先保证手机可用：桌面可用 Grid/Flex 多列布局，在约 720px 以下收为单列；控件可点击区域不小于约 40px，内容不得横向溢出。
- 交互须提供悬停、键盘焦点、禁用、加载、成功/失败反馈；避免自动跳转、无确认的破坏性操作和影响阅读的持续动画。

## VuePress 与变更边界

- 未经用户明确允许，不得修改 VuePress 核心配置和基础设施：`src/.vuepress/config.js`、`src/.vuepress/client.js`、主题别名/覆盖组件、全局样式、构建脚本、`package.json` 与锁文件。
- 不得为单个工具添加全局 CSS、插件、路由配置或构建配置；目录约定和自动发现机制应保持不变。
- 修改完成后，优先运行与改动相称的检查；工具改动至少确认构建不报错，并检查桌面与移动端布局及深浅色主题。
